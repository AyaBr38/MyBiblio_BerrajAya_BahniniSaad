<h2 class="page-header"><%= author.name %></h2>
<div class="book-details-label">
  <div><%= author.description %></div>
</div>
<br>
<div class="btn-row">
<a class="btn btn-primary" href="/authors/<%= author.id %>/edit">Modifier</a>
<%- include('../partials/deleteForm',
            { url: `/authors/${author.id}` }) %>
</div>
<% if (booksByAuthor.length > 0) { %>
  <h2 class="page-header">Livres par cet auteur</h2>
  <%- include('../partials/bookGrid2', {books:booksByAuthor}) %>
<% } %>